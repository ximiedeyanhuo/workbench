# better-web-ui · Project Design Context

## Design Context

### Users
自托管个人工作台的单一主人（开发者本人）+ 可能的家庭成员多用户（服务器按用户隔离数据库）。
使用场景：每天早上打开看「今天要做什么」，碎片时间记账/打卡/速记，晚上复盘。
核心任务：今日待办 → 打卡 → 记账 → 翻资讯 → 周末看报表。高频页面：仪表盘、事务、记账、资讯。

### Brand Personality
温暖、克制、可靠（一本随身携带的小账本，不是企业驾驶舱）。
情绪目标：平静的掌控感——打开就知道今天该干什么，不被信息淹没。

### Aesthetic Direction
- 莫兰迪 Soft UI：低饱和暖橙（杏色/琥珀）为唯一强调色，奶油纸底色 + 深色候车厅模式
- 反参考：禁止紫色渐变 SaaS 模板感、企业后台管理系统感、Midjourney 式"现代仪表盘"
- 圆角适度（radius-sm 为主），轻阴影，emoji 仅用于语义装饰不用于操作按钮
- 数据一律等宽数字（tabular-nums）
- 明暗分明：亮色是印刷纸感，暗色是深夜灰蓝，对比度 ≥4.5:1（已验证）

### Implementation Defaults
- **硬性约束：零构建零依赖**——原生 JS Hash-SPA（全局 WB 命名空间）+ 原生 CSS 变量 token，不引入打包器/框架/组件库/Tailwind
- css/app.css 单文件 ~5000 行，token 体系已建立（--bg/--card/--ink/--muted/--accent/--ok/--danger/--rise/--fall + 6 套主题）
- 图表 Chart.js（已本地化 lib/），表单原生 + WB.flashInvalid() 校验
- 表格/表单/虚拟化：全部手写原生实现，保持现状
- sw.js CACHE 版本号在改 js/css 后必须递增（workbench-vNN）

### Design Principles
1. **间距宽松优先**——宁可留白多一点，不要紧凑（用户多次明确反馈"太紧凑"）
2. **先层级后颜色**——用间距/字重/分组建立层级，颜色只用于语义（涨跌/收支/状态），不做装饰性配色
3. **一个页面一个主行动**——每屏只有一个明显的主动作（记账/添加/打卡），其余用 ghost 弱化
4. **数据可扫读**——数字等宽右对齐、列表密度服务于快速扫视，卡片不套卡片
5. **动效即反馈**——动画只用于状态反馈（完成/保存/切换），时长 ≤300ms，不做入场表演
6. **不破坏数据与流程**——UI 改造不动业务逻辑/API/schema，改完必须实测交互回归
